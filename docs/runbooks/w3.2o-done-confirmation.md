# W3.2o Done Confirmation

Date: 2026-04-24
`main` at: pending squash commit from the Forum de l'Officine PR

Against
[`docs/superpowers/specs/2026-04-24-medevents-w3-2o-forum-officine-tn.md`](../superpowers/specs/2026-04-24-medevents-w3-2o-forum-officine-tn.md)
§6:

## §6.1 — Parser registered + config seeded

- New file `services/ingest/medevents_ingest/parsers/forum_officine.py`
  registered as `parser_name = 'forum_officine_tn'`.
- `services/ingest/medevents_ingest/parsers/__init__.py` updated to
  side-effect import the Forum de l'Officine parser alongside the earlier
  curated-source parsers.
- `config/sources.yaml` entry added for `forum_officine_tn` with:
  - `homepage_url = https://www.forumdelofficine.tn/l_officine/accueil-forum-officine.php`
  - two seed URLs (homepage + practical-information page)
  - `source_type = other`
  - `crawl_frequency = monthly`
  - edition-rollover note in `notes:`

## §6.2 — Fixtures + prep review captured

Supporting prep artifacts committed:

- [`docs/runbooks/forum-officine-fixtures.md`](forum-officine-fixtures.md)
- [`services/ingest/tests/fixtures/forum_officine_tn/home.html`](../../services/ingest/tests/fixtures/forum_officine_tn/home.html)
- [`services/ingest/tests/fixtures/forum_officine_tn/infos-pratiques.html`](../../services/ingest/tests/fixtures/forum_officine_tn/infos-pratiques.html)
- [`services/ingest/tests/fixtures/forum_officine_tn/robots.txt`](../../services/ingest/tests/fixtures/forum_officine_tn/robots.txt)

Prep result: robots acceptable, both chosen pages are byte-stable, the public
2026 event contract is explicit, and raw-body hashing is sufficient without a
custom normalizer. The prep review also documents the intentional parser rule
to ignore the stale assistant-widget May 1-2 session/FAQ data and trust the
page-level metadata + Schema.org `Event` JSON-LD instead.

## §6.3 — 8 new tests pass (6 unit + 2 DB-gated)

```text
$ cd services/ingest && UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q tests/test_forum_officine_parser.py
6 passed in 2.80s

$ cd services/ingest && TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q tests/test_forum_officine_pipeline.py
2 passed in 13.44s
```

## §6.4 — Repo-wide verification

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q
204 passed in 34.07s
```

Additional local gates:

- `ruff check .` -> clean
- `mypy .` -> `Success: no issues found in 71 source files`

## §6.5 — Live smoke against the official Forum pages

Seed refresh:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest seed-sources --path ../../config/sources.yaml
upserted 11 source(s) from ../../config/sources.yaml
```

First run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest run --source forum_officine_tn
source=forum_officine_tn fetched=2 skipped_unchanged=0 created=1 updated=1 review_items=0
```

Final rerun:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest run --source forum_officine_tn
source=forum_officine_tn fetched=2 skipped_unchanged=2 created=0 updated=0 review_items=0
```

Interpretation target met:

- first run creates one canonical event row and the second `event_sources` row
- unchanged rerun reports `skipped_unchanged=2`
- no review items are emitted

## §6.6 — Dev DB snapshot after live smoke

```text
$ psql -c "SELECT e.title, e.starts_on, e.ends_on, e.city, e.country_iso, \
    e.venue_name, e.source_url, e.registration_url, count(es.*) AS event_sources ..."
          title           | starts_on  |  ends_on   | city  | country_iso |         venue_name          |                              source_url                               |                                           registration_url                                           | event_sources
--------------------------+------------+------------+-------+-------------+-----------------------------+-----------------------------------------------------------------------+------------------------------------------------------------------------------------------------------+---------------
 Forum de l'Officine 2026 | 2026-05-15 | 2026-05-16 | Tunis | TN          | Palais des Congres de Tunis | https://www.forumdelofficine.tn/l_officine/accueil-forum-officine.php | https://main.d17j5ouws4ciim.amplifyapp.com/formulaires/congressiste/3f6d7b9c1a2e4f5g6h7j8k9m0n1p2q3r |             2

$ psql -c "SELECT code, last_crawled_at IS NOT NULL, last_success_at IS NOT NULL ..."
       code        | has_last_crawled_at | has_last_success_at
-------------------+---------------------+---------------------
 forum_officine_tn | t                   | t
```

## §6.7 — Project state after merge

After this PR lands:

- mainline ingests **eleven curated sources**: `ada`, `gnydm`,
  `aap_annual_meeting`, `fdi_wdc`, `eao_congress`, `cds_midwinter`,
  `dentex_algeria`, `morocco_dental_expo`, `europerio`, `amied_congress`,
  and `forum_officine_tn`
- W3.2d live Fly deploy remains the only required project-level blocker
- the next optional expansion candidate becomes the generic fallback parser or
  another curated-source wave
