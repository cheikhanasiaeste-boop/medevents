# W3.2k Done Confirmation

Date: 2026-04-24
`main` at: pending squash commit from the Dentex PR

Against [`docs/superpowers/specs/2026-04-24-medevents-w3-2k-dentex-algeria.md`](../superpowers/specs/2026-04-24-medevents-w3-2k-dentex-algeria.md) §6:

## §6.1 — Parser registered + config seeded

- New file `services/ingest/medevents_ingest/parsers/dentex.py` registered as `parser_name = 'dentex_algeria'`.
- `services/ingest/medevents_ingest/parsers/__init__.py` updated to side-effect import Dentex alongside ADA/GNYDM/AAP/FDI/EAO/CDS.
- `config/sources.yaml` entry added for `dentex_algeria` with:
  - `homepage_url = https://www.dentex.dz/en/`
  - two seed URLs (homepage + visit page)
  - `source_type = other`
  - `crawl_frequency = monthly`
  - edition-rollover note in `notes:`

## §6.2 — Fixtures + prep review captured

Supporting prep artifacts committed:

- [`docs/runbooks/dentex-fixtures.md`](dentex-fixtures.md)
- [`services/ingest/tests/fixtures/dentex/homepage.html`](../../services/ingest/tests/fixtures/dentex/homepage.html)
- [`services/ingest/tests/fixtures/dentex/visit.html`](../../services/ingest/tests/fixtures/dentex/visit.html)
- [`services/ingest/tests/fixtures/dentex/robots.txt`](../../services/ingest/tests/fixtures/dentex/robots.txt)

Prep result: robots acceptable, both source surfaces stable enough for raw-body hashing, and the committed fixtures preserve the real 2026 title/date/venue/CTA contract after nonce sanitization.

## §6.3 — 8 new tests pass (6 unit + 2 DB-gated)

```text
$ cd services/ingest && uv run pytest -q tests/test_dentex_parser.py
6 passed

$ cd services/ingest && TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    uv run pytest -q tests/test_dentex_pipeline.py
2 passed
```

## §6.4 — Repo-wide verification at 171 passed

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    uv run pytest -q
171 passed in 265.06s
```

Additional local gates:

- `ruff check .` → clean
- `mypy .` → `Success: no issues found in 59 source files`

## §6.5 — Live smoke against the official Dentex site

Seed refresh:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest seed-sources --path ../../config/sources.yaml
upserted 7 source(s) from ../../config/sources.yaml
```

First run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source dentex_algeria
source=dentex_algeria fetched=2 skipped_unchanged=0 created=1 updated=1 review_items=0
```

Re-run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source dentex_algeria
source=dentex_algeria fetched=2 skipped_unchanged=2 created=0 updated=0 review_items=0
```

Interpretation:

- first run created one canonical event row and the second `event_sources` row
- an immediate second live rerun briefly reported `created=0 updated=2`, but two consecutive follow-up reruns both reported `skipped_unchanged=2`, so the final stable reruns are the evidence-of-record
- no review items were emitted

## §6.6 — Dev DB snapshot after live smoke

```text
$ psql -c "SELECT e.title, e.starts_on, e.ends_on, e.city, e.country_iso, \
    e.venue_name, e.registration_url, count(es.*) AS event_sources ..."
        title         | starts_on  |  ends_on   |  city   | country_iso |                  venue_name                  |              registration_url               | event_sources
----------------------+------------+------------+---------+-------------+----------------------------------------------+---------------------------------------------+---------------
 DENTEX Algeria 2026  | 2026-06-02 | 2026-06-05 | Algiers | DZ          | Algiers Exhibition Center - SAFEX (...)      | https://register.visitcloud.com/survey/...  |             2

$ psql -c "SELECT code, last_crawled_at IS NOT NULL, last_success_at IS NOT NULL ..."
      code       | has_last_crawled_at | has_last_success_at
-----------------+---------------------+---------------------
 dentex_algeria  | t                   | t
```

One event row, two `event_sources` rows, canonical homepage `source_url`, and source bookkeeping populated.

## §6.7 — Project state after merge

After this PR lands:

- mainline ingests **seven curated sources**: `ada`, `gnydm`, `aap_annual_meeting`, `fdi_wdc`, `eao_congress`, `cds_midwinter`, `dentex_algeria`
- W3.2d live Fly deploy remains the only required project-level blocker
- the next optional expansion candidate becomes the generic fallback parser, `morocco_dental_expo`, or another curated-source wave
