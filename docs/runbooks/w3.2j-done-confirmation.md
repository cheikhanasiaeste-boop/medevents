# W3.2j Done Confirmation

Date: 2026-04-24
`main` at: pending squash commit from the CDS PR

Against [`docs/superpowers/specs/2026-04-24-medevents-w3-2j-cds-midwinter.md`](../superpowers/specs/2026-04-24-medevents-w3-2j-cds-midwinter.md) §6:

## §6.1 — Parser registered + config seeded

- New file `services/ingest/medevents_ingest/parsers/cds.py` registered as `parser_name = 'cds_midwinter'`.
- `services/ingest/medevents_ingest/parsers/__init__.py` updated to side-effect import CDS alongside ADA/GNYDM/AAP/FDI/EAO.
- `config/sources.yaml` entry added for `cds_midwinter` with:
  - `homepage_url = https://www.cds.org/event/2026-midwinter-meeting/`
  - two seed URLs (public event page + public JSON endpoint)
  - `crawl_frequency = monthly`
  - edition-rollover note in `notes:`

## §6.2 — Fixtures + prep review captured

Supporting prep artifacts committed:

- [`docs/runbooks/cds-fixtures.md`](cds-fixtures.md)
- [`services/ingest/tests/fixtures/cds/event.html`](../../services/ingest/tests/fixtures/cds/event.html)
- [`services/ingest/tests/fixtures/cds/event.json`](../../services/ingest/tests/fixtures/cds/event.json)
- [`services/ingest/tests/fixtures/cds/robots.txt`](../../services/ingest/tests/fixtures/cds/robots.txt)

Prep result: robots acceptable, both source surfaces byte-stable, raw-body hashing safe.

## §6.3 — 8 new tests pass (6 unit + 2 DB-gated)

```text
$ cd services/ingest && uv run pytest -q tests/test_cds_parser.py
6 passed

$ cd services/ingest && TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    uv run pytest -q tests/test_cds_pipeline.py
2 passed
```

## §6.4 — Repo-wide verification at 163 passed

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    uv run pytest -q
163 passed in 33.21s
```

Additional local gates:

- `ruff check .` → clean
- `mypy .` → `Success: no issues found in 56 source files`

## §6.5 — Live smoke against the official CDS site

Seed refresh:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest seed-sources --path ../../config/sources.yaml
upserted 6 source(s) from ../../config/sources.yaml
```

First run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source cds_midwinter
source=cds_midwinter fetched=2 skipped_unchanged=0 created=1 updated=1 review_items=0
```

Re-run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source cds_midwinter
source=cds_midwinter fetched=2 skipped_unchanged=2 created=0 updated=0 review_items=0
```

Interpretation:

- first run created one canonical event row and the second `event_sources` row
- second run skipped both pages unchanged on the real site
- no review items were emitted

## §6.6 — Dev DB snapshot after live smoke

```text
$ psql -c "SELECT e.title, e.starts_on, e.ends_on, e.city, e.country_iso, \
    e.venue_name, e.timezone, e.registration_url, count(es.*) AS event_sources ..."
                     title                     | starts_on  |  ends_on   |  city   | country_iso |      venue_name      |    timezone     |             registration_url              | event_sources
-----------------------------------------------+------------+------------+---------+-------------+----------------------+-----------------+-------------------------------------------+---------------
 Chicago Dental Society Midwinter Meeting 2026 | 2026-02-19 | 2026-02-21 | Chicago | US          | McCormick Place West | America/Chicago | https://midwintermeeting.eventscribe.net/ |             2

$ psql -c "SELECT code, last_crawled_at IS NOT NULL, last_success_at IS NOT NULL ..."
     code      | has_last_crawled_at | has_last_success_at
---------------+---------------------+---------------------
 cds_midwinter | t                   | t
```

One event row, two `event_sources` rows, venue/timezone enrichment present, and source bookkeeping populated.

## §6.7 — Project state after merge

After this PR lands:

- mainline ingests **six curated sources**: `ada`, `gnydm`, `aap_annual_meeting`, `fdi_wdc`, `eao_congress`, `cds_midwinter`
- W3.2d live Fly deploy remains the only required project-level blocker
- the next optional expansion candidate becomes the generic fallback parser or another curated source wave
