# W3.2h Done Confirmation

Date: 2026-04-24
`main` at: pending squash commit from the FDI PR

Against [`docs/superpowers/specs/2026-04-24-medevents-w3-2h-fdi-world-dental-congress.md`](../superpowers/specs/2026-04-24-medevents-w3-2h-fdi-world-dental-congress.md) §6:

## §6.1 — Parser registered + config seeded

- New file `services/ingest/medevents_ingest/parsers/fdi.py` registered as `parser_name = 'fdi_wdc'`.
- `services/ingest/medevents_ingest/parsers/__init__.py` updated to side-effect import FDI alongside ADA/GNYDM/AAP.
- `config/sources.yaml` entry added for `fdi_wdc` with:
  - `homepage_url = https://www.fdiworlddental.org/fdi-world-dental-congress`
  - two seed URLs (hub + 2026 detail)
  - `crawl_frequency = monthly`
  - edition-rollover note in `notes:`

## §6.2 — Fixtures + prep review captured

Supporting prep artifacts committed:

- [`docs/runbooks/fdi-fixtures.md`](fdi-fixtures.md)
- [`services/ingest/tests/fixtures/fdi/hub.html`](../../services/ingest/tests/fixtures/fdi/hub.html)
- [`services/ingest/tests/fixtures/fdi/wdc-2026.html`](../../services/ingest/tests/fixtures/fdi/wdc-2026.html)
- [`services/ingest/tests/fixtures/fdi/wdc-2025.html`](../../services/ingest/tests/fixtures/fdi/wdc-2025.html)
- [`services/ingest/tests/fixtures/fdi/robots.txt`](../../services/ingest/tests/fixtures/fdi/robots.txt)

Prep result: robots acceptable, pages byte-stable, raw-body hashing safe.

## §6.3 — 8 new tests pass (6 unit + 2 DB-gated)

```text
$ cd services/ingest && uv run pytest -q tests/test_fdi_parser.py
6 passed

$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    uv run pytest -q tests/test_fdi_pipeline.py
2 passed
```

## §6.4 — Repo-wide verification at 147 passed

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    uv run pytest -q
147 passed in 123.32s (0:02:03)
```

Additional local gates:

- `ruff check .` → clean
- `mypy .` → `Success: no issues found in 50 source files`

## §6.5 — Live smoke against the official FDI site

Seed refresh:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest seed-sources --path ../../config/sources.yaml
upserted 4 source(s) from ../../config/sources.yaml
```

First run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source fdi_wdc
source=fdi_wdc fetched=2 skipped_unchanged=0 created=1 updated=1 review_items=0
```

Re-run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source fdi_wdc
source=fdi_wdc fetched=2 skipped_unchanged=2 created=0 updated=0 review_items=0
```

Interpretation:

- first run created one canonical event row and a second `event_sources` row
- second run skipped both pages unchanged on the real site
- no review items were emitted

## §6.6 — Dev DB snapshot after live smoke

```text
$ psql -c "SELECT e.title, e.starts_on, e.ends_on, e.city, e.country_iso, \
    e.registration_url, count(es.*) AS event_sources ..."
             title              | starts_on  |  ends_on   |  city  | country_iso |            registration_url             | event_sources
--------------------------------+------------+------------+--------+-------------+-----------------------------------------+---------------
 FDI World Dental Congress 2026 | 2026-09-04 | 2026-09-07 | Prague | CZ          | https://2026.world-dental-congress.org/ |             2

$ psql -c "SELECT code, last_crawled_at IS NOT NULL, last_success_at IS NOT NULL ..."
  code   | has_last_crawled_at | has_last_success_at
---------+---------------------+---------------------
 fdi_wdc | t                   | t
```

One event row, two `event_sources` rows, and source bookkeeping populated.

## §6.7 — Project state after merge

After this PR lands:

- mainline ingests **four curated sources**: `ada`, `gnydm`, `aap_annual_meeting`, `fdi_wdc`
- W3.2d live Fly deploy remains the only required project-level blocker
- the next optional expansion candidate becomes `eao_congress` or, later, the generic fallback parser
