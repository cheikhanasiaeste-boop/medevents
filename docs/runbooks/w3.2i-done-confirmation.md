# W3.2i Done Confirmation

Date: 2026-04-24
`main` at: pending squash commit from the EAO PR

Against [`docs/superpowers/specs/2026-04-24-medevents-w3-2i-eao-congress.md`](../superpowers/specs/2026-04-24-medevents-w3-2i-eao-congress.md) §6:

## §6.1 — Parser registered + config seeded

- New file `services/ingest/medevents_ingest/parsers/eao.py` registered as `parser_name = 'eao_congress'`.
- `services/ingest/medevents_ingest/parsers/__init__.py` updated to side-effect import EAO alongside ADA/GNYDM/AAP/FDI.
- `config/sources.yaml` entry added for `eao_congress` with:
  - `homepage_url = https://eao.org/congress/`
  - two seed URLs (hub + 2026 microsite homepage)
  - `crawl_frequency = monthly`
  - microsite-rollover note in `notes:`

## §6.2 — Fixtures + prep review captured

Supporting prep artifacts committed:

- [`docs/runbooks/eao-fixtures.md`](eao-fixtures.md)
- [`services/ingest/tests/fixtures/eao/hub.html`](../../services/ingest/tests/fixtures/eao/hub.html)
- [`services/ingest/tests/fixtures/eao/homepage.html`](../../services/ingest/tests/fixtures/eao/homepage.html)
- [`services/ingest/tests/fixtures/eao/programme-detail.html`](../../services/ingest/tests/fixtures/eao/programme-detail.html)
- [`services/ingest/tests/fixtures/eao/robots.txt`](../../services/ingest/tests/fixtures/eao/robots.txt)

Prep result:

- main-domain robots are permissive
- microsite homepage is byte-stable
- hub raw HTML is not byte-stable, but the root cause is understood and normalized in-parser

## §6.3 — 8 new tests pass (6 unit + 2 DB-gated)

```text
$ uv run pytest -q services/ingest/tests/test_eao_parser.py
6 passed

$ DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    uv run pytest -q services/ingest/tests/test_eao_pipeline.py
2 passed
```

## §6.4 — Repo-wide verification at 155 passed

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    uv run pytest -q
155 passed in 30.90s
```

Additional local gates:

- `ruff check .` → clean
- `mypy .` → `Success: no issues found in 53 source files`

## §6.5 — Live smoke against the official EAO hub + 2026 microsite

Seed refresh:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest seed-sources --path ../../config/sources.yaml
upserted 5 source(s) from ../../config/sources.yaml
```

First run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source eao_congress
source=eao_congress fetched=2 skipped_unchanged=0 created=3 updated=1 review_items=0
```

During live verification, the first unchanged rerun exposed a second rotating hub fragment (the LiteSpeed footer comment) in addition to the already-normalized Simple Banner timestamps. `parsers/eao.py::_normalize_body_for_hashing()` was widened accordingly, then verification was repeated:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source eao_congress
source=eao_congress fetched=2 skipped_unchanged=1 created=0 updated=3 review_items=0

$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    uv run medevents-ingest run --source eao_congress
source=eao_congress fetched=2 skipped_unchanged=2 created=0 updated=0 review_items=0
```

Interpretation:

- first run created the three canonical EAO congress rows and the second `event_sources` row for 2026
- the intermediate rerun refreshed the stored hub hash after the normalization widening
- the final rerun skipped both pages unchanged on the real site
- no review items were emitted

## §6.6 — Dev DB snapshot after live smoke

```text
$ psql -c "SELECT e.title, e.starts_on, e.ends_on, e.city, e.country_iso, \
    e.registration_url, count(es.*) AS event_sources ..."
       title       | starts_on  |  ends_on   |   city    | country_iso |                 registration_url                  | event_sources
-------------------+------------+------------+-----------+-------------+---------------------------------------------------+---------------
 EAO Congress 2026 | 2026-09-24 | 2026-09-26 | Lisbon    | PT          | https://congress.eao.org/en/congress/registration |             2
 EAO Congress 2027 | 2027-09-23 | 2027-09-25 | Madrid    | ES          |                                                   |             1
 EAO Congress 2028 | 2028-10-19 | 2028-10-21 | Amsterdam | NL          |                                                   |             1

$ psql -c "SELECT code, last_crawled_at IS NOT NULL, last_success_at IS NOT NULL ..."
     code     | has_last_crawled_at | has_last_success_at
--------------+---------------------+---------------------
 eao_congress | t                   | t
```

Three event rows, four `event_sources` rows total, and source bookkeeping populated.

## §6.7 — Project state after merge

After this PR lands:

- mainline ingests **five curated sources**: `ada`, `gnydm`, `aap_annual_meeting`, `fdi_wdc`, `eao_congress`
- W3.2d live Fly deploy remains the only required project-level blocker
- the next optional expansion candidate becomes the generic fallback parser or another curated source wave
