# W3.2m Done Confirmation

Date: 2026-04-24
`main` at: pending squash commit from the EuroPerio PR

Against
[`docs/superpowers/specs/2026-04-24-medevents-w3-2m-europerio.md`](../superpowers/specs/2026-04-24-medevents-w3-2m-europerio.md)
§6:

## §6.1 — Parser registered + config seeded

- New file `services/ingest/medevents_ingest/parsers/europerio.py`
  registered as `parser_name = 'europerio'`.
- `services/ingest/medevents_ingest/parsers/__init__.py` updated to
  side-effect import EuroPerio alongside the earlier curated-source parsers.
- `config/sources.yaml` entry added for `europerio` with:
  - `homepage_url = https://www.efp.org/europerio/`
  - two seed URLs (hub + EuroPerio12 detail page)
  - `source_type = society`
  - `crawl_frequency = monthly`
  - edition-rollover note in `notes:`

## §6.2 — Fixtures + prep review captured

Supporting prep artifacts committed:

- [`docs/runbooks/europerio-fixtures.md`](europerio-fixtures.md)
- [`services/ingest/tests/fixtures/europerio/hub.html`](../../services/ingest/tests/fixtures/europerio/hub.html)
- [`services/ingest/tests/fixtures/europerio/europerio12.html`](../../services/ingest/tests/fixtures/europerio/europerio12.html)
- [`services/ingest/tests/fixtures/europerio/robots.txt`](../../services/ingest/tests/fixtures/europerio/robots.txt)

Prep result: robots acceptable, both chosen pages are byte-stable, the 2028
event contract is explicit, and raw-body hashing is sufficient without a
custom normalizer.

## §6.3 — 8 new tests pass (6 unit + 2 DB-gated)

```text
$ cd services/ingest && UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q tests/test_europerio_parser.py
6 passed

$ cd services/ingest && TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q tests/test_europerio_pipeline.py
2 passed
```

## §6.4 — Repo-wide verification

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q
188 passed in 101.62s (0:01:41)
```

Additional local gates:

- `ruff check .` → clean
- `mypy .` → `Success: no issues found in 65 source files`

## §6.5 — Live smoke against the official EFP EuroPerio pages

Seed refresh:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest seed-sources --path ../../config/sources.yaml
upserted 9 source(s) from ../../config/sources.yaml
```

First run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest run --source europerio
source=europerio fetched=2 skipped_unchanged=0 created=1 updated=1 review_items=0
```

Final rerun:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest run --source europerio
source=europerio fetched=2 skipped_unchanged=2 created=0 updated=0 review_items=0
```

Interpretation target:

- first run creates one canonical event row and the second `event_sources` row
- unchanged rerun reports `skipped_unchanged=2`
- no review items are emitted

## §6.6 — Dev DB snapshot after live smoke

```text
$ psql -c "SELECT e.title, e.starts_on, e.ends_on, e.city, e.country_iso, \
    e.source_url, count(es.*) AS event_sources ..."
    title    | starts_on  |  ends_on   |  city  | country_iso |                 source_url                 | event_sources
-------------+------------+------------+--------+-------------+--------------------------------------------+---------------
 EuroPerio12 | 2028-05-10 | 2028-05-13 | Munich | DE          | https://www.efp.org/europerio/europerio12/ |             2

$ psql -c "SELECT code, last_crawled_at IS NOT NULL, last_success_at IS NOT NULL ..."
   code    | has_last_crawled_at | has_last_success_at
-----------+---------------------+---------------------
 europerio | t                   | t
```

## §6.7 — Project state after merge

After this PR lands:

- mainline ingests **nine curated sources**: `ada`, `gnydm`,
  `aap_annual_meeting`, `fdi_wdc`, `eao_congress`, `cds_midwinter`,
  `dentex_algeria`, `morocco_dental_expo`, and `europerio`
- W3.2d live Fly deploy remains the only required project-level blocker
- the next optional expansion candidate becomes the generic fallback parser or
  another curated-source wave
