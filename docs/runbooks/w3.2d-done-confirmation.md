# W3.2d Done Confirmation

Date: _operator fills in after first successful scheduled run_
`main` at: _sha where w3.2d completion is committed_

Against [`docs/superpowers/specs/2026-04-23-medevents-w3-2d-fly-scheduled-machines.md`](../superpowers/specs/2026-04-23-medevents-w3-2d-fly-scheduled-machines.md) §5:

## §5.1 — Dockerfile builds locally

_Operator paste of `docker build -t medevents-ingest:dev services/ingest` final line, e.g. `naming to docker.io/library/medevents-ingest:dev`._

```text
<paste build success output>
```

## §5.2 — Image runs `--help` without error

```text
$ docker run --rm medevents-ingest:dev --help
<paste Typer help output>
```

## §5.3 — `fly.toml` exists with scheduled-machine config

Confirmed in [`services/ingest/fly.toml`](../../services/ingest/fly.toml) on `main`. Contains:

- `app = "<operator-app-name>"`
- `[build] dockerfile = "Dockerfile"`
- `[[machines]]` with `schedule = "hourly"`

## §5.4 — Deploy runbook exists + operator completed

[`docs/runbooks/w3.2d-fly-scheduler-deploy.md`](w3.2d-fly-scheduler-deploy.md) contains the step-by-step. Operator executed steps 1–7 on _date_.

First scheduled run log snippet (from `fly logs --app <app-name>`):

```text
<paste the batch=run-all ... line and any per-source lines from the first autonomous run>
```

## §5.5 — Admin UI shows real timestamps from the autonomous run

SQL probe or screenshot showing `sources.last_crawled_at` set to the timestamp of the first scheduled Fly run:

```text
$ psql -c "SELECT code, last_crawled_at, last_success_at FROM sources ORDER BY code;"
 code  |     last_crawled_at      |     last_success_at
-------+--------------------------+--------------------------
 ada   | <fly run timestamp>      | <fly run timestamp>
 gnydm | <fly run timestamp>      | <fly run timestamp>
```

## §5.6 — State / TODO updated

- `docs/state.md` — mark W3.2d ✅ Complete. Point restart notes at W3.2e.
- `docs/TODO.md` — move W3.2d bullet to Shipped; promote W3.2e (`aap_annual_meeting`) to "Now".

---

## Cost actuals (fill in after a full month of scheduled runs)

| Month   | Fly machine | Fly Postgres | External PG | Total |
| ------- | ----------- | ------------ | ----------- | ----- |
| 2026-05 | _$_         | _$_          | _$_         | _$_   |

---

## Next-wave readiness

Once this runbook is filled in and committed:

- W3.2e (third source — `aap_annual_meeting`) is unblocked. The third source's Parser + config entry land on `main`; the already-deployed Fly scheduled machine picks it up on the next wake without any infra changes.
- No Fly redeploy needed to add sources — the image's `config/sources.yaml` is updated in a normal PR, the machine rebuilds the image on next deploy (whether triggered by merge or a nightly refresh).
