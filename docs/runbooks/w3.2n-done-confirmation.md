# W3.2n Done Confirmation

Date: 2026-04-24
`main` at: pending squash commit from the AMIED congress PR

Against
[`docs/superpowers/specs/2026-04-24-medevents-w3-2n-amied-congress.md`](../superpowers/specs/2026-04-24-medevents-w3-2n-amied-congress.md)
§6:

## §6.1 — Parser registered + config seeded

- New file `services/ingest/medevents_ingest/parsers/amied.py`
  registered as `parser_name = 'amied_congress'`.
- `services/ingest/medevents_ingest/parsers/__init__.py` updated to
  side-effect import the AMIED parser alongside the earlier curated-source
  parsers.
- `config/sources.yaml` entry added for `amied_congress` with:
  - `homepage_url = https://amied.ma/`
  - two seed URLs (homepage + inscriptions page)
  - `source_type = society`
  - `crawl_frequency = monthly`
  - edition-rollover note in `notes:`

## §6.2 — Fixtures + prep review captured

Supporting prep artifacts committed:

- [`docs/runbooks/amied-fixtures.md`](amied-fixtures.md)
- [`services/ingest/tests/fixtures/amied/home.html`](../../services/ingest/tests/fixtures/amied/home.html)
- [`services/ingest/tests/fixtures/amied/inscriptions.html`](../../services/ingest/tests/fixtures/amied/inscriptions.html)
- [`services/ingest/tests/fixtures/amied/robots.txt`](../../services/ingest/tests/fixtures/amied/robots.txt)

Prep result: robots acceptable, both chosen pages are byte-stable, the public
2026 event contract is explicit, and raw-body hashing is sufficient without a
custom normalizer. The committed inscriptions fixture strips bank-transfer
identifiers that are irrelevant to parsing.

## §6.3 — 8 new tests pass (6 unit + 2 DB-gated)

```text
$ cd services/ingest && UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q tests/test_amied_parser.py
6 passed in 5.09s

$ cd services/ingest && TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q tests/test_amied_pipeline.py
2 passed in 15.39s
```

## §6.4 — Repo-wide verification

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q
196 passed in 48.66s
```

Additional local gates:

- `ruff check .` -> clean
- `mypy .` -> `Success: no issues found in 68 source files`

## §6.5 — Live smoke against the official AMIED pages

Seed refresh:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest seed-sources --path ../../config/sources.yaml
upserted 10 source(s) from ../../config/sources.yaml
```

First run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest run --source amied_congress
source=amied_congress fetched=2 skipped_unchanged=0 created=1 updated=1 review_items=0
```

Final rerun:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest run --source amied_congress
source=amied_congress fetched=2 skipped_unchanged=2 created=0 updated=0 review_items=0
```

Interpretation target met:

- first run creates one canonical event row and the second `event_sources` row
- unchanged rerun reports `skipped_unchanged=2`
- no review items are emitted

## §6.6 — Dev DB snapshot after live smoke

```text
$ psql -c "SELECT e.title, e.starts_on, e.ends_on, e.city, e.country_iso, \
    e.venue_name, e.source_url, e.registration_url, count(es.*) AS event_sources ..."
               title               | starts_on  |  ends_on   |   city    | country_iso |           venue_name           |    source_url     |                                                 registration_url                                                  | event_sources
-----------------------------------+------------+------------+-----------+-------------+--------------------------------+-------------------+-------------------------------------------------------------------------------------------------------------------+---------------
 AMIED International Congress 2026 | 2026-06-19 | 2026-06-20 | Marrakech | MA          | Barcelo Palmeraie Oasis Resort | https://amied.ma/ | https://docs.google.com/forms/d/e/1FAIpQLSd3x-i-F-pC42oIUyNEJ9qXvJYKqhZTKrrztW5hkYJQ5WC7_w/viewform?embedded=true |             2

$ psql -c "SELECT code, last_crawled_at IS NOT NULL, last_success_at IS NOT NULL ..."
      code      | has_last_crawled_at | has_last_success_at
----------------+---------------------+---------------------
 amied_congress | t                   | t
```

## §6.7 — Project state after merge

After this PR lands:

- mainline ingests **ten curated sources**: `ada`, `gnydm`,
  `aap_annual_meeting`, `fdi_wdc`, `eao_congress`, `cds_midwinter`,
  `dentex_algeria`, `morocco_dental_expo`, `europerio`, and `amied_congress`
- W3.2d live Fly deploy remains the only required project-level blocker
- the next optional expansion candidate becomes the generic fallback parser or
  another curated-source wave
