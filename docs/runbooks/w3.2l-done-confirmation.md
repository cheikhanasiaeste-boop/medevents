# W3.2l Done Confirmation

Date: 2026-04-24
`main` at: pending squash commit from the Morocco Dental Expo PR

Against
[`docs/superpowers/specs/2026-04-24-medevents-w3-2l-morocco-dental-expo.md`](../superpowers/specs/2026-04-24-medevents-w3-2l-morocco-dental-expo.md)
§6:

## §6.1 — Parser registered + config seeded

- New file
  `services/ingest/medevents_ingest/parsers/morocco_dental_expo.py`
  registered as `parser_name = 'morocco_dental_expo'`.
- `services/ingest/medevents_ingest/parsers/__init__.py` updated to
  side-effect import Morocco Dental Expo alongside ADA/GNYDM/AAP/FDI/EAO/CDS/Dentex.
- `config/sources.yaml` entry added for `morocco_dental_expo` with:
  - `homepage_url = https://www.mdentalexpo.ma/lang/en`
  - two seed URLs (homepage + exhibitor list)
  - `source_type = other`
  - `crawl_frequency = monthly`
  - edition-rollover note in `notes:`

## §6.2 — Fixtures + prep review captured

Supporting prep artifacts committed:

- [`docs/runbooks/morocco-dental-expo-fixtures.md`](morocco-dental-expo-fixtures.md)
- [`services/ingest/tests/fixtures/morocco_dental_expo/homepage.html`](../../services/ingest/tests/fixtures/morocco_dental_expo/homepage.html)
- [`services/ingest/tests/fixtures/morocco_dental_expo/exhibitor-list.html`](../../services/ingest/tests/fixtures/morocco_dental_expo/exhibitor-list.html)
- [`services/ingest/tests/fixtures/morocco_dental_expo/robots.txt`](../../services/ingest/tests/fixtures/morocco_dental_expo/robots.txt)

Prep result: robots acceptable, the homepage plus exhibitor-list page define a
clean 2026 contract, the English practical-information page was explicitly
rejected as stale, and hash normalization for the rotating ASP.NET hidden
fields proved sufficient.

## §6.3 — 9 new tests pass (7 unit + 2 DB-gated)

```text
$ cd services/ingest && UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q tests/test_morocco_dental_expo_parser.py
7 passed

$ cd services/ingest && TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q tests/test_morocco_dental_expo_pipeline.py
2 passed
```

## §6.4 — Repo-wide verification at 180 passed

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    TEST_DATABASE_URL=postgresql://…@localhost:5432/medevents_test \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run pytest -q
180 passed in 37.20s
```

Additional local gates:

- `ruff check .` → clean
- `mypy .` → `Success: no issues found in 62 source files`

## §6.5 — Live smoke against the official Morocco Dental Expo site

Seed refresh:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest seed-sources --path ../../config/sources.yaml
upserted 8 source(s) from ../../config/sources.yaml
```

First run:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest run --source morocco_dental_expo
source=morocco_dental_expo fetched=2 skipped_unchanged=0 created=1 updated=1 review_items=0
```

Final reruns:

```text
$ cd services/ingest && DATABASE_URL=postgresql://…@localhost:5432/medevents \
    UV_CACHE_DIR=/tmp/uv-cache \
    uv run medevents-ingest run --source morocco_dental_expo
source=morocco_dental_expo fetched=2 skipped_unchanged=2 created=0 updated=0 review_items=0
```

Interpretation:

- first run created one canonical event row and the second `event_sources` row
- two final unchanged reruns both reported `skipped_unchanged=2`
- the ASP.NET hidden-field normalization held on the real site, not just the
  fixtures
- no review items were emitted

## §6.6 — Dev DB snapshot after live smoke

```text
$ psql -c "SELECT e.title, e.starts_on, e.ends_on, e.city, e.country_iso, \
    e.venue_name, e.registration_url, e.source_url, count(es.*) AS event_sources ..."
          title           | starts_on  |  ends_on   |    city    | country_iso |   venue_name   |                 registration_url                 |             source_url             | event_sources
--------------------------+------------+------------+------------+-------------+----------------+--------------------------------------------------+------------------------------------+---------------
 Morocco Dental Expo 2026 | 2026-05-07 | 2026-05-10 | Casablanca | MA          | ICEC AIN SEBAA | https://www.mdentalexpo.ma/form/2749?cat=VISITOR | https://www.mdentalexpo.ma/lang/en |             2

$ psql -c "SELECT code, last_crawled_at IS NOT NULL, last_success_at IS NOT NULL ..."
        code         | has_last_crawled_at | has_last_success_at
---------------------+---------------------+---------------------
 morocco_dental_expo | t                   | t
```

One event row, two `event_sources` rows, canonical homepage `source_url`,
venue populated from the exhibitor-list page, homepage registration URL
preserved, and source bookkeeping populated.

## §6.7 — Project state after merge

After this PR lands:

- mainline ingests **eight curated sources**: `ada`, `gnydm`,
  `aap_annual_meeting`, `fdi_wdc`, `eao_congress`, `cds_midwinter`,
  `dentex_algeria`, `morocco_dental_expo`
- W3.2d live Fly deploy remains the only required project-level blocker
- the next optional expansion candidate becomes the generic fallback parser,
  `europerio`, or another curated-source wave
